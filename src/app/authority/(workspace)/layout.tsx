import AuthorityShell from "./authority-shell";

export default function AuthorityWorkspaceLayout({ children }: { children: React.ReactNode }) {
  return <AuthorityShell>{children}</AuthorityShell>;
}
